# 数组方法拓展

## Array.prototype.indexOf

```js
var beasts = ['ant', 'bison', 'camel', 'duck', 'bison'];

console.log(beasts.indexOf('bison'));
// expected output: 1

// start from index 2
console.log(beasts.indexOf('bison', 2));
// expected output: 4

console.log(beasts.indexOf('giraffe'));
// expected output: -1
```

`**indexOf()**`方法返回在数组中可以找到一个给定元素的第一个索引，如果不存在，则返回-1。

## 数组转化

```javascript
    function dis(){
        /**
         * 将伪数组转化为真数组
         * */
        Array.from(arguments).forEach((item,index)=>{
            console.log(item)
            console.log(index)
        })
    }
    dis(1,5,7,6,2)
	//数组拼接
    let arr2 = Array.of(1,'sda',true,2112)

    console.log(arr2)

```

## 数组索值

#### 找条件值：

```javascript
    let min = arr2.find(function(item,index){
            return item == true
    })
    console.log(min)

    let min2 = arr2.findIndex(function(item,index){
            return item == true
    })
    console.log(min2)
```

#### 找最大值：

```javascript
let array = [4,6,7,8,9,1,0];
Math.max.call(window/null,array);
```

## 存在性问题

```javascript
let arr = [1,2,5,6,'abc']
console.log(arr.includes('abc'))
```

## 其余

```javascript
<script> 
    var arr = [9,8,7,6,5,4,3,2,1]
/**
  * 输出数组中值为5的元素的下标
  */
    console.log(arr.indexOf(5))

/**
  * 遍历当前数组，没有返回值
  */
    var arr1 = arr.forEach(function(item,index){
            //console.log('下标:'+index+'值:'+item)
            item++
            return item
    })

/**
  * map根据原数组重构一个新数组，回调函数里要return每一个键上的新值。
  */
    var arr1 = arr.map(function(item,index){
            item++
            if(item>6){
                return item
            }
    })
    //[10, 9, 8, 7, undefined, undefined, undefined, undefined, undefined]
    console.log(arr1)

/**
  * filter过滤原数组
  */
    var arr2 = arr.filter(function(item,index){
            return item>6
    })
    //[9, 8, 7]
    console.log(arr2)

</script>
<script>
    let arr = [6,1,2,4,3]

    let newarr = arr.filter(function(item,index){
          return item > 3
    })

    console.log(newarr)
</script>
```

